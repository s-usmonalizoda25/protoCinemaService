syntax = "proto3";

package v1;

option go_package = "github.com/s-usmonalizoda25/bookingServiceCinemaProject/bookingpb/v1;v1";

service BookingService {
    rpc CreateBooking(CreateBookingRequest) returns (CreateBookingResponse);
    rpc GetBooking(GetBookingRequest) returns (GetBookingResponse);
    rpc GetUserBookings(GetUserBookingsRequest) returns (GetUserBookingsResponse);
    rpc CancelBooking(CancelBookingRequest) returns (CancelBookingResponse);
}

enum BookingStatus {
    BOOKING_STATUS_UNSPECIFIED = 0;
    BOOKING_STATUS_PENDING = 1;
    BOOKING_STATUS_CONFIRMED = 2;
    BOOKING_STATUS_CANCELED = 3;
}

message Booking {
    int64 id = 1;
    int64 user_id = 2;
    int64 movie_id = 3;
    BookingStatus status = 4;
}

message CreateBookingRequest {
    int64 user_id = 1;
    int64 movie_id = 2;
}   

message CreateBookingResponse {
    Booking booking = 1;
}

message GetBookingRequest {
    int64 id = 1;
}

message GetBookingResponse {
    Booking booking = 1;
}

message GetUserBookingsRequest {
    int64 user_id = 1;
}

message GetUserBookingsResponse {
    repeated Booking bookings = 1;
}

message CancelBookingRequest {
    int64 id = 1;
}

message CancelBookingResponse {
    bool success = 1;
}